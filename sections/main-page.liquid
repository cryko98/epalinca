{% render 'zk-page-head', title: page.title %}

<div class="wrap zk-body">
  <div class="zk-rte">{{ page.content }}</div>
</div>

{% schema %}
{"name": "Main Pagina", "settings": []}
{% endschema %}  {%- if page.metafields.zekulhyda.faq.value != blank -%}<div class="wrap">{% render 'zk-faq', items: page.metafields.zekulhyda.faq.value %}</div>{%- endif -%}

